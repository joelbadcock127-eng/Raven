# Raven event-monitor → Supabase sync
# Project: joel@bakerswalkingco.au's Project (org "Acceso ai 2")
SUPABASE_URL=https://agjvvuqndhzzvexdwhle.supabase.co
# Service role key (Dashboard → Project Settings → API keys → service_role).
# Server-side only — never expose in a browser or commit it.
SUPABASE_SERVICE_ROLE_KEY=
